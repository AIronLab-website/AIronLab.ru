/**
 * Auth Layout
 * 
 * Layout для страниц аутентификации (login)
 * Не включает sidebar и header
 */

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return children
}

